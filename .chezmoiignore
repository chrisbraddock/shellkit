# shellkit chezmoiignore
# Files to skip during chezmoi apply

# Development/testing files
shellkit-test.zsh
README.md
LICENSE
init.sh
install.sh
scripts/
cliff.toml
VERSION
CHANGELOG.md

# Editor/IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Git (chezmoi has its own version control)
.git/
.gitignore

# Claude/bluera tooling
.claude/
.bluera/
.ignore/

# Build artifacts
*.log
*.tmp

# Local customization files (machine-specific, not synced)
.zshrc.local

# Profile-based ignores (minimal profile skips antidote plugin list)
{{ if eq .profile "minimal" }}
dot_zsh_plugins.txt
{{ end }}

# Work identity disabled - skip work gitconfig
{{ if not .identities.work.enabled }}
dot_gitconfig-work.tmpl
{{ end }}
